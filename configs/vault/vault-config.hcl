# ===========================================
# Vault Configuration
# ===========================================

ui = true

# In dev mode (-dev) Vault manages its own in-memory storage and listener
# (via VAULT_DEV_LISTEN_ADDRESS). No listener/storage blocks here.

# API address
api_addr = "http://127.0.0.1:8200"

# Log level
log_level = "info"